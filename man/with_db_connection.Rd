% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/with-db-connection.R
\name{with_db_connection}
\alias{with_db_connection}
\title{Use and automatically close a DBI connection}
\usage{
with_db_connection(connection_provider, code)
}
\arguments{
\item{connection_provider}{A zero-argument function returning a valid
\link[DBI:DBIConnection-class]{DBI::DBIConnection}.}

\item{code}{A function taking the connection as its only argument.}
}
\value{
The value returned by \code{code}.
}
\description{
Obtains a connection from \code{connection_provider}, passes it to \code{code}, and
closes it when \code{code} finishes. The connection is also closed when \code{code}
raises an error. A connection that has already been closed by \code{code} is not
closed a second time.
}
\details{
\code{with_db_connection()} owns the connection returned by the provider. Do not
use it when the provider returns a shared connection whose lifecycle is
managed elsewhere.
}
\examples{
if (requireNamespace("RSQLite", quietly = TRUE)) {
  connection_provider <- function() {
    DBI::dbConnect(RSQLite::SQLite(), ":memory:")
  }

  with_db_connection(connection_provider, function(connection) {
    DBI::dbGetQuery(connection, "SELECT 1 AS value")
  })
}
}
