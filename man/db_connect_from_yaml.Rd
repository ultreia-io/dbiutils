% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-connect-from-yaml.R
\name{db_connect_from_yaml}
\alias{db_connect_from_yaml}
\title{Connect to a database using YAML configuration}
\usage{
db_connect_from_yaml(drv, config_file)
}
\arguments{
\item{drv}{A \link[DBI:DBIDriver-class]{DBI::DBIDriver} object created by the consuming
application.}

\item{config_file}{A path to a UTF-8 YAML file containing named connection
arguments.}
}
\value{
A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}.
}
\description{
Reads connection arguments from a YAML file and passes them to
\code{\link[DBI:dbConnect]{DBI::dbConnect()}}. The YAML document must contain a top-level mapping whose
keys are connection argument names supported by the selected DBI driver.
}
\details{
Values tagged with \code{!env} are read from environment variables. For example,
\verb{password: !env APPLICATION_DB_PASSWORD} keeps the password out of the YAML
file. An error is raised if the referenced variable is not set.

For local development, the optional \code{keyring} package can retrieve a secret
from the operating system credential store. Copy that value into the
referenced environment variable for the duration of the R session, then
unset the variable when the database work is complete. CI and production
should inject the same variable from their secret manager.

\code{dbiutils} does not select or import a database driver. The consuming
application supplies the driver and remains responsible for installing it.
}
\examples{
if (requireNamespace("RSQLite", quietly = TRUE)) {
  config_file <- tempfile(fileext = ".yaml")
  writeLines("dbname: ':memory:'", config_file, useBytes = TRUE)

  connection <- db_connect_from_yaml(RSQLite::SQLite(), config_file)
  DBI::dbDisconnect(connection)
}
}
